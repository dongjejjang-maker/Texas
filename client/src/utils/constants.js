export const SERVER_URL = window.location.origin;

// 필요 시 다른 설정값들을 여기에 추가할 수 있습니다.
export const DEFAULT_BUY_IN = 20000;
export const DEFAULT_SB = 100;
export const DEFAULT_BB = 200;
export const MAX_PLAYERS_LIMIT = 8;
